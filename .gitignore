# Generated outputs and downloads
outputs/
output/
downloads/
dist/

.env

# Large local datasets and benchmark artifacts
dataset/pre-selecao-top4-bitrate/
dataset/**/*.mp4
dataset/**/*.mov
dataset/**/*.mkv
dataset/**/*.webm
dataset/**/*.y4m

# Video and transcoding artifacts
*.mp4
*.mov
*.mkv
*.webm
*.y4m
*.avi
*.m4v
*.ts

# Local binaries and reports from ad-hoc runs
transcode-local
# ...but keep the command's source package (the bare pattern above also matched
# the cmd/transcode-local/ dir, leaving the Batch entrypoint untracked).
!cmd/transcode-local/
coverage.out

# macOS noise
.DS_Store

# air (hot reload em make development-infra)
tmp/
